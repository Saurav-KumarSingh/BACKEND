<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    
    <div class="flex justify-end p-4"><a class="px-4 py-1 text-white bg-red-500 rounded-md " href="/logout">Logout</a></div>
    <div class="flex justify-between">
        <div class="w-1/3 text-center">
            <form action="/post" method="post">
                <textarea rows="5" name="content" class="resize-none m-4 border-2 rounded w-full p-4"
                    placeholder="write something here"></textarea>
                <button type="submit" class="px-5 py-2 bg-blue-400 rounded-md text-white">create post</button>
            </form>
            
        </div>
        <div class="w-1/2 my-8">
            <div class="flex flex-col items-center gap-4">

                <!-- Avatar with Gradient Shadow -->
                <div class="relative my-4">
                    <img src="/images/uploads/<%=user.profilepic%>"
                        class="relative object-cover  shrink-0 h-28 w-28 z-10 rounded-full" alt="profile">
                </div>

                <p class="text-2xl font-bold">
                    <%=user.name%>
                </p>

                <div class="text-gray-400">
                    <%= user.email %>
                </div>

            </div>
        </div>

    </div>
    <div class="mt-6">
        <h3 class="my-4 text-2xl font-bold text-start mx-4">Posts</h3>
        <div class="post-container p-4 text-start flex gap-4">
            
            <% user.posts.forEach((post)=>{ %>
                <div class="card p-4 border-2 rounded-md w-1/3">
                    <p class=" text-blue-400 pb-4">@<%=user.username %></p>
                    <p class="tracking-tighter"><%= post.content %></p>
                </div>
            <% }) %>
            
        </div>
    </div>
</body>

</html>